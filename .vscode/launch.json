{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Portfolio (Chrome)",
            "type": "pwa-chrome",
            "request": "launch",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}
